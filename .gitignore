# Data & outputs (never commit: real exported/donated conversation data is
# ERB-restricted and must not leave secured storage; outputs/ is derived
# from it). A handful of synthetic sample files under data/ are tracked
# anyway via `git add -f` as pipeline fixtures — this rule still protects
# any *real* export you drop in the same data/<platform>/user_0/ paths.
data/

# Secrets
.env
.env.*

# OS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual envs
.venv/
venv/
env/

# Notebooks
.ipynb_checkpoints/

# Editors
.vscode/
.idea/
